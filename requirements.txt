# =====================================================================
# 맛집 추천 ReAct Agent - 실행 환경
# =====================================================================
# 기본 실행은 추가 패키지 없이 Python 3.9+ 표준 라이브러리만으로 동작합니다.
# (json, math, re 만 사용 → 키 없이 바로 실행 가능)
#
# 아래 패키지는 "선택" 기능을 켤 때만 필요합니다. 설치해도 키가 없으면
# 자동으로 오프라인 모드(샘플 데이터 + 규칙기반 추론)로 동작합니다.

openai>=1.0.0

requests>=2.28.0
